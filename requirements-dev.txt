# Runtime lock plus the tools CI runs. Platform-only helpers (colorama, pefile
# on Windows) are resolved by pip.
-r requirements.txt
coverage==7.16.1
mypy==2.3.1
pyinstaller==6.22.3
pyinstaller-hooks-contrib==2026.7
pytest==9.1.1
pytest-cov==7.1.0
ruff==0.16.7
