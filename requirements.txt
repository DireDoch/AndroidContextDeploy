# Exact versions CI and the release bundle are built with (Python 3.12).
# The supported ranges live in pyproject.toml. To refresh: install the ranges
# in a clean venv, then `pip freeze --exclude-editable`.
adbutils==2.12.0
av==18.1.0
certifi==2026.7.22
charset-normalizer==3.5.1
customtkinter==5.2.2
darkdetect==0.8.0
decorator==5.3.1
deprecation==2.1.0
idna==3.19
numpy==2.5.3
packaging==26.3
pillow==12.3.0
requests==2.34.2
retry2==0.9.5
urllib3==2.7.0
